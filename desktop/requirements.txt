# Core dependencies
python-docx>=1.1.0
mammoth>=1.8.0
markitdown[pdf,docx,xlsx,xls,pptx]>=0.1.5
pymupdf>=1.24.0
requests>=2.31.0
wxPython>=4.2.0

# Build/packaging (install separately for builds)
# pip install pyinstaller>=6.0
