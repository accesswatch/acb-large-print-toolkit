# Temporary commit message files
msg.txt

# Python
__pycache__/
*.py[cod]
*$py.class
*.egg-info/
*.egg
dist/
build/
*.spec

# PyInstaller
desktop/dist/
desktop/build/
desktop/installer/version_info.txt
dist/

# Virtual environments
.venv/
venv/
env/

# IDE
.vscode/
.idea/
*.swp
*.swo
*~

# OS
.DS_Store
Thumbs.db
Desktop.ini

# Local history (VS Code extension)
.history/

# Secrets and credentials
.env
*.credentials

# Logs
*.log

# Runtime SQLite databases (created automatically on first use)
instance/visitor_counter.db
instance/tool_usage.db

# Speech model files (large binaries, managed outside the repo)
web/instance/speech_models/
*.onnx
*.onnx.json

# Local runtime artifacts and state databases
instance/*.db
instance/.cleanup_lock
instance/.cleanup_lock.tmp
web/instance/*.db
web/instance/*.db-shm
web/instance/*.db-wal
web/instance/.cleanup_lock
web/instance/.cleanup_lock.tmp
web/e2e/instance/*.db

# Local secret material (never commit)
glow-*-firebase-adminsdk-*.json
web/instance/firebase-service-account.json

# Local scratch exports/reports
web/post_findings.json

# Temporary migration and diagnostics workspace
tmp/**
